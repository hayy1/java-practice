package com.company.practice;

public class DB {

    /*
    * SELECT                      5
    * FROM --- 1 BEST             1
    * WHERE                       2
    * GROUP BY                    3
    * HAVING                      4
    * ORDER BY                    6
    *
    * */

    // JOIN

    // INNER JOIN, OUTER JOIN

    // R-DB (Relation DB) vs NO SQL (NO Relation DB)

    // R-DB = (Oracle, MySQL, MariaDB, MSSQL)

    // NO-SQL = (Redis, Cassandra, mongoDB)

    // 분류 =  정규화

    // 데이터의 무결성은 중복이 없는 것



}
